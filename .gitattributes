# Auto detect text files and perform LF normalization
* text=auto

# Force LF for shell scripts (must not be converted to CRLF)
*.sh text eol=lf

# Force CRLF for Windows batch files
*.bat text eol=crlf
*.ps1 text eol=crlf
